# Test- and lint-time dependencies. Not installed by the venv bootstrap.
# Install with: .venv/bin/pip install -r dev-requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
ruff>=0.6,<1.0
mypy>=1.10,<2.0
